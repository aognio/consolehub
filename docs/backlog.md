# ConsoleHub Backlog

## Completed
- [x] TOML configuration module (`internal/config`).
- [x] Embedded PocketBase schema initialization (`internal/storage`).
- [x] Session auth engine & HMAC signing (`internal/auth`).
- [x] Domain services with sequence tracking & deduplication (`internal/services`).
- [x] JSON-RPC 2.0 over WebSocket telemetry ingestion (`internal/api/jsonrpc`).
- [x] Real-time PubSub hub & SSE broadcaster (`internal/stream`).
- [x] Web UI templates & responsive Tailwind CSS layout (`internal/templates`, `internal/ui`).
- [x] Flagship Console Viewer (`/runs/:id/console`).

## Future Items
- [ ] Lightweight client daemon agent (`consolehub-agent`).
- [ ] Process remote control capabilities.
